spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  rabbitmq:
    order-queue: "inventory.order-created.queue"
    order-exchange: "orders.events.exchange"
    order-routing-key: "orders.created"